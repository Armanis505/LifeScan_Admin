const LifeScanTitle = ({ size }) => {
    return (
        <>
            <span className={`${size} font-bold text-white tracking-tight`}>LifeScan<span className="text-emerald-500">Admin</span></span>
        </>
    )
}

export default LifeScanTitle;